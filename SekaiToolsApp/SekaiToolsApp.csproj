<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <!-- Avalonia 桌面应用，作为 SekaiTools 唯一 UI 入口。
             目标 net8.0：与共享库一致，可在 Windows + macOS 编译运行。 -->
        <OutputType>WinExe</OutputType>
        <TargetFramework>net10.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
        <RootNamespace>SekaiToolsApp</RootNamespace>
        <ApplicationManifest>app.manifest</ApplicationManifest>
        <AvaloniaUseCompiledBindingsByDefault>true</AvaloniaUseCompiledBindingsByDefault>
        <BuiltInComInteropSupport>true</BuiltInComInteropSupport>
        <Configurations>Debug;Release</Configurations>
        <Platforms>AnyCPU;x64</Platforms>
        <UseAppHost>true</UseAppHost>
        <RuntimeIdentifiers>win-x64;osx-x64;osx-arm64;linux-x64</RuntimeIdentifiers>
        <Version>1.0.1-pre</Version>
        <ApplicationIcon>Assets\icon.ico</ApplicationIcon>
        <NoWarn>$(NoWarn);CA1416</NoWarn>
        <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
        <!-- 当前 SDK 下 app 的项目引用图并行恢复/清理会失败，先关掉并行保证稳定。 -->
        <BuildInParallel>false</BuildInParallel>
    </PropertyGroup>

    <PropertyGroup Condition="'$(Configuration)' == 'Debug'">
        <DefineConstants>$(DefineConstants);DEBUG;TRACE</DefineConstants>
    </PropertyGroup>

    <ItemGroup>
        <AvaloniaResource Include="Assets\**" />
        <Content Include="Resources\lim5994.vpy" CopyToOutputDirectory="PreserveNewest" CopyToPublishDirectory="PreserveNewest" />
        <!-- 字幕默认字体随应用发布，供压制 fontsdir 使用（说明见 SekaiToolsEngine.csproj；
             OFL 许可证放应用根目录、不进 fonts/，免得 libass 把 txt 当字体加载报噪音） -->
        <Content Include="..\resource\fonts\**" Link="fonts\%(Filename)%(Extension)"
                 CopyToOutputDirectory="PreserveNewest" CopyToPublishDirectory="PreserveNewest" />
        <Content Include="..\resource\LICENSE-SourceHanSans.txt" Link="LICENSE-SourceHanSans.txt"
                 CopyToOutputDirectory="PreserveNewest" CopyToPublishDirectory="PreserveNewest" />
    </ItemGroup>

    <ItemGroup>
        <PackageReference Include="Avalonia" Version="11.2.3" />
        <PackageReference Include="Avalonia.Desktop" Version="11.2.3" />
        <PackageReference Include="Avalonia.Themes.Fluent" Version="11.2.3" />
        <PackageReference Include="Avalonia.Fonts.Inter" Version="11.2.3" />
        <PackageReference Include="Avalonia.ReactiveUI" Version="11.2.3" />
        <PackageReference Condition="'$(Configuration)' == 'Debug'" Include="Avalonia.Diagnostics" Version="11.2.3" />
        <PackageReference Include="FluentAvaloniaUI" Version="2.2.0" />
        <PackageReference Include="CommunityToolkit.Mvvm" Version="8.4.0" />
        <PackageReference Include="Microsoft.Extensions.DependencyInjection" Version="9.0.0" />
    </ItemGroup>

    <ItemGroup>
        <ProjectReference Include="..\SekaiToolsCore\SekaiToolsCore.csproj" />
        <ProjectReference Include="..\SekaiToolsBase\SekaiToolsBase.csproj" />
        <ProjectReference Include="..\SekaiDataFetch\SekaiDataFetch.csproj" />
        <ProjectReference Include="..\SekaiToolsPlatform\SekaiToolsPlatform.csproj" />
    </ItemGroup>

</Project>
